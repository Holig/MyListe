// Stub pour le build web : évite l'erreur d'import du plugin mobile_scanner
class MobileScannerWeb {
  static void registerWith(dynamic registrar) {
    // Ne rien faire, c'est un stub pour le web
  }
} 